# Rust build output
/target

# Legacy state directory of the retired TypeScript implementation. A live
# orchestrator is still running out of it while the Rust rewrite is carried
# out — do not remove this entry yet.
.pi-fleet/
node_modules
# pilotfish
.pilotfish/
# the previous name's state; a fleet may still be running out of it
.parl/
.lavish/
.zed/
